# Daily Code Review — 2026-05-29

**Range:** `9428f52..4c3b5e3` (2 commits)

## Summary

Pure bookkeeping range: two squash merges of the bot's own PRs (#148
daily review, #149 daily test audit). The combined diff is four files
under `docs/reviews/` — two new markdown reports plus the two
state-JSON bumps that go with them. Zero source diff (`apps/web/src/**`,
`crates/**`, `*.py`, `*.ts`, `*.tsx`, `*.rs`), zero test diff, zero
build/config touch, no new dependencies. Both reports themselves are
accurate and internally consistent with the ranges they claim to cover;
the only carry-over items worth re-noting are the same two low-severity
docs-drift / bookkeeping nits the previous review flagged: `CLAUDE.md`
still contradicts `docs/book/11-run-test-ship.md` on whether the wasm
bundle is checked in, and `test-audit-state.json` is still writing
`"pr_url": null` for an audit that was in fact merged via PR #149.
Neither was introduced by this range; both remain unresolved.

## Findings

### Critical

None.

### High

None.

### Medium

None.

### Low

- **`CLAUDE.md:42-43` vs `docs/book/11-run-test-ship.md:72-74` and
  `.gitignore:19-20`** — unchanged docs-drift, surfaced now by a third
  bookkeeping cycle that did not address it. `CLAUDE.md` still says
  *"The generated bundle in `apps/web/src/wasm/` **is checked into
  git** so editors, CI, and tests work without `wasm-pack` — don't add
  it to `.gitignore`."* Ground truth: `.gitignore:19-20` ignores
  `apps/web/src/wasm/*` except `.gitkeep`, and the book chapter
  correctly says *"The wasm generated files live at
  `apps/web/src/wasm/` and are gitignored; run `pnpm wasm:build` (or
  `:dev`) on a fresh checkout and after any `crates/` change."* This
  was flagged in 2026-05-26-review and 2026-05-28-review, openly
  acknowledged in `docs/sync-logs/2026-05-28-docs-sync.md:27` as
  outside the sync allowlist. It is now the canonical onboarding hazard
  for any new contributor reading `CLAUDE.md` first. **Fix:** human
  edit on `CLAUDE.md:41-43` flipping the claim to match the book
  chapter and `.gitignore`. Not actionable inside the docs-only
  bookkeeping range under review, but worth re-flagging until it lands.

- **`docs/reviews/test-audit-state.json:4`** — `"pr_url": null` for an
  audit that was just merged via PR #149 (commit subject of `4c3b5e3`
  in the merge range itself). Same shape as the previous review's Low
  finding for PR #146. The file is written before the PR exists and
  never backfilled. Two possibilities: drop the field (the workflow
  doesn't actually track it post-merge) or backfill it from the squash
  commit's PR number. Current state is a permanent
  `null`-after-the-fact that conflicts with the commit subject one line
  away in git history.

### Nits

- **2026-05-29 test audit report** (`docs/reviews/2026-05-29-test-audit.md:3`)
  states the range as `df8e1ee..2a21930` (3 commits). The actual
  audit-state diff in the same PR was bumping `last_audited_sha` from
  `df8e1ee` to `2a21930`, which is the same range — fine. But the
  three commits in that audit window are themselves the previous
  test-audit merge (`3e97967`), the docs-sync (`9428f52`), and the
  previous code-review merge (`2a21930`). The audit's "Range" wording
  is technically correct but cosmetically inconsistent with how the
  *current* daily-review report scopes things by parent commit. Not a
  bug, but if the two bookkeeping bots ever cross-reference their
  ranges in a single PR description, the off-by-one is going to
  confuse a reader. Pure process polish.

## Per-commit notes

- **`2a21930`** chore(review): daily code review 2026-05-28 (#148) —
  Adds `docs/reviews/2026-05-28-review.md` (124 lines) and bumps
  `daily-review-state.json` from `7fe2372` → `9428f52`. The report's
  two Low findings (CLAUDE.md drift and `pr_url: null`) are accurate;
  its Nit on `docs/sync-logs/2026-05-28-docs-sync.md` is fair process
  polish. No source effect.

- **`4c3b5e3`** test: daily test audit 2026-05-29 (#149) — Adds
  `docs/reviews/2026-05-29-test-audit.md` (80 lines) and bumps
  `test-audit-state.json` from `df8e1ee` → `2a21930` (note: not to the
  PR's own merge SHA — that's expected, the audit ran before this
  squash merge existed). The audit's baseline numbers — 447 vitest +
  67 cargo = 514 passing — are consistent with the previous two
  audits and look unchanged. No tests added or modified, correctly so
  for a zero-source-diff range.

## Stats

- Files changed: 4
- Lines: +209 / -5 (net +204)
- Production source touched (`apps/web/src/**`, `crates/**/src/**`,
  `*.ts`, `*.tsx`, `*.rs`, `*.py`): 0
- Test files touched: 0
- New markdown reports: 2 (`2026-05-28-review.md`,
  `2026-05-29-test-audit.md`)
- JSON state files bumped: 2 (daily-review-state, test-audit-state)
- New dependencies: 0
- Net: no behaviour change to ship; carry-over docs-drift unresolved.
