# Daily Code Review — 2026-05-26

**Range:** `5ae6974..554f0ba` (3 commits)

## Summary

The range is documentation-only by file count, but it lands a new
`CLAUDE.md` (`cf4700b`, +126 lines) that is intended to be read as
system-prompt context by AI agents — and **four of its factual claims
contradict the current repo**, the worst being that the generated WASM
bundle in `apps/web/src/wasm/` is "checked into git … don't add it to
`.gitignore`," when `.gitignore:19-20` actually ignores
`apps/web/src/wasm/*` and the directory holds only `.gitkeep`. CLAUDE.md
also points at a non-existent `apps/web/src/index.css`, a non-existent
`crates/data-core/src/readers/` directory, and invents Chrome/Edge/
Firefox 130+ version floors that contradict `unsupportedSplash.ts`
(94+, and Firefox unsupported). The two review/audit bookkeeping
commits (`2768b97`, `554f0ba`) are clean docs-only updates with
verified stat figures.

## Findings

### Critical

None.

### High

- **`CLAUDE.md:41-44` vs `.gitignore:19-20` — wasm bundle is *not*
  checked into git.** CLAUDE.md asserts: "The generated bundle in
  `apps/web/src/wasm/` **is checked into git** so editors, CI, and
  tests work without `wasm-pack` — don't add it to `.gitignore`."
  Reality: `.gitignore` contains `apps/web/src/wasm/*` with only
  `!apps/web/src/wasm/.gitkeep` un-ignored, and `ls apps/web/src/wasm/`
  returns just `.gitkeep`. Because CLAUDE.md is loaded as agent context
  via `claudeMd`, this misdirects every subsequent agent run: an agent
  reading the doc will believe builds Just Work, miss the
  `pnpm wasm:build` prerequisite under the "IMPORTANT" callout right
  above, and may even try to "fix" the wasm/* gitignore line as if it
  were a mistake. **Fix:** rewrite the paragraph to match policy —
  state that `apps/web/src/wasm/` is gitignored and that
  `pnpm wasm:build` (or `:dev`) must be run on a fresh checkout and
  after any change in `crates/`.

### Medium

- **`CLAUDE.md:75-76` — `apps/web/src/index.css` does not exist.** The
  "Code conventions / CSS" bullet reads "No global styles outside
  `apps/web/src/index.css`." There is no such file; the global token
  sheet is `apps/web/src/styles/tokens.css`, which
  `docs/06-ui-and-panels.md:9-12` already documents as the single
  source of truth. **Fix:** point CLAUDE.md at
  `apps/web/src/styles/tokens.css` (or `apps/web/src/styles/`).

- **`CLAUDE.md:97` — `crates/data-core/src/readers/` does not exist.**
  The "What lives where" section claims "`crates/data-core/src/readers/`
  — one module per format." Actual layout is flat:
  `crates/data-core/src/{mcap,mf4,mp4_sidecar,reader}.rs`. An agent
  trying to navigate to reader code via this path will get a 404.
  **Fix:** drop the `readers/` subdirectory and list the actual files,
  or — if the long-term intent is to introduce a `readers/` mod — that
  is a code change, not a docs change.

- **`CLAUDE.md:86-87` — WebCodecs version floors are invented and
  Firefox support is misstated.** CLAUDE.md says "WebCodecs is
  **required** for video (Chrome/Edge 130+, Firefox 130+). Safari is
  unsupported by design." But `apps/web/src/unsupportedSplash.ts:19-29`
  advertises "Google Chrome 94+", "Microsoft Edge 94+", and explicitly
  states: "Firefox and Safari do not yet ship the WebCodecs video APIs
  Driveline needs." Firefox is unsupported, not 130+-supported, and
  the Chromium floor is 94+ not 130+. Either CLAUDE.md or the splash
  is wrong; the in-tree behavioural truth is the splash. **Fix:**
  rewrite CLAUDE.md's browser line to match the splash (Chrome/Edge
  94+, Firefox unsupported, Safari unsupported), or — if the project
  has genuinely moved to Chromium 130+ and added Firefox support —
  update the splash and the underlying detection in
  `isWebCodecsSupported`.

### Low

- **`docs/reviews/2026-05-25-review.md:3` — "5 commits, 2 merges"
  double-reads.** The range header reads `**Range:**` `f8c0284..5ae6974`
  (5 commits, 2 merges). Two of the five *are* merge commits, but the
  parenthetical reads as "5 + 2 = 7" on a first pass. Earlier reports
  in this series use the unambiguous form "N commits". Cosmetic.

### Nits

- **`CLAUDE.md:5` — `@README.md` / `@docs/` reference syntax.** The
  leading `@` is not used by any other doc in the repo and isn't a
  valid GitHub/markdown link, so the references render as literal
  text rather than navigable links. Drop the `@` or rewrite as
  markdown links.

- **`docs/reviews/2026-05-26-test-audit.md` — baseline test counts
  inherited.** The audit reports `cargo` 67 / vitest 447 / 38 web test
  files unchanged from prior audits, which is policy-correct for a
  docs-only range but those numbers were not re-run in this cycle.
  Not a finding for this review; flagging so they aren't treated as
  fresh measurements by readers downstream.

## Per-commit notes

- **`2768b97`** chore(review): daily code review 2026-05-25
  (`f8c0284..5ae6974`) (#137). Squash-merged. Adds
  `docs/reviews/2026-05-25-review.md` (+91) and bumps
  `daily-review-state.json` to `5ae6974` /
  `2026-05-25T03:06:40Z`. Cross-checked: `git diff --shortstat
  f8c0284..5ae6974` returns `4 files changed, 161 insertions(+), 6
  deletions(-)`, matching the report's own "+161/−6" claim, and
  `git diff --shortstat 8ae25e0..f8c0284` returns `+145/−6`, matching
  the secondary spot-check in that report. Wording nit captured in
  Low #1. Otherwise clean.

- **`cf4700b`** docs: add `CLAUDE.md` with project context for AI
  agents (#138). Squash-merged. Adds the only new top-level file in
  the range. **Carries findings High #1 and Medium #1–3.** The intent
  is sound — capturing rules ("never `parseInt` a BigInt timestamp",
  "one Zustand store, no Context providers", size budgets, hot-path
  discipline) and pointing agents at the deeper docs/skill — but the
  factual slips are exactly the kind of details an AI agent will rely
  on without double-checking, so they need correcting.

- **`554f0ba`** test: daily test audit 2026-05-26
  (`07f8220..cf4700b`) (#139). Squash-merged. Adds
  `docs/reviews/2026-05-26-test-audit.md` (+74) and bumps
  `test-audit-state.json` to `cf4700b` /
  `2026-05-26T02:07:04Z` with `pr_url: null`. Range-classification is
  correct (CLAUDE.md is documentation, the rest is review/audit
  bookkeeping → zero coverage gaps). Inherited baseline counts noted
  in Nits #2. Otherwise clean.

## Stats

- Commits in range: 3 (all squash-merged via PRs #137, #138, #139)
- Files changed: 5 (1 new top-level doc, 2 new review/audit reports,
  2 state JSONs)
- Inserted: +297
- Deleted: −6
- Source files (`apps/*/src`, `crates/*/src`): 0
- Test files touched: 0 (`*.test.ts(x)`, `crates/**/tests`,
  `apps/e2e/**`: all untouched)
- New dependencies: none (`package.json`, `pnpm-lock.yaml`,
  `Cargo.toml`, `Cargo.lock`: all untouched)
- Docs drift introduced: 4 factual errors in CLAUDE.md (see High #1,
  Medium #1–3).
