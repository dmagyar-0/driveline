# Daily Code Review — 2026-05-28

**Range:** `7fe2372..9428f52` (3 commits)

## Summary

Pure bookkeeping range: yesterday's review and audit PRs merged, plus
today's docs-sync commit. Zero source or test diff in `apps/web/src/**`,
`crates/**`, or any `*.py`. The only substantive edit is a one-paragraph
correction in `docs/book/11-run-test-ship.md` flipping the wasm bundle's
ownership claim from "checked in" to "gitignored" — which matches
`.gitignore:19-20` (only `apps/web/src/wasm/.gitkeep` is tracked). The
docs-sync deliberately left `CLAUDE.md` carrying the identical wrong
claim, so the agent context and the book chapter now actively contradict
each other on a question new contributors are guaranteed to hit
(`pnpm dev` failing without `pnpm wasm:build`). Worth a one-line fix on
`CLAUDE.md:41-44`. One other low-severity bookkeeping nit: today's audit
state file lost its `pr_url` field.

## Findings

### Critical

None.

### High

None.

### Medium

None.

### Low

- **`CLAUDE.md:41-44` vs `docs/book/11-run-test-ship.md:72-75`** —
  docs-drift the sync explicitly noted but didn't fix. Today's commit
  `9428f52` rewrote the book chapter to say the wasm output is
  gitignored and must be rebuilt via `pnpm wasm:build`. CLAUDE.md still
  says the opposite: "The generated bundle in `apps/web/src/wasm/` **is
  checked into git** so editors, CI, and tests work without `wasm-pack`
  — don't add it to `.gitignore`." The actual ground truth is
  `.gitignore:19-20` (`apps/web/src/wasm/*` ignored, `.gitkeep`
  re-included) and `git ls-files apps/web/src/wasm/` returning only the
  keepfile. The sync log
  (`docs/sync-logs/2026-05-28-docs-sync.md:13,27`) openly acknowledges
  CLAUDE.md sits outside its allowlist, so the sync isn't broken — but
  the result is that the project's two onboarding surfaces tell opposite
  stories. **Fix:** edit `CLAUDE.md:41-44` to mirror the book chapter
  ("generated bundle in `apps/web/src/wasm/` is **gitignored** — run
  `pnpm wasm:build` (or `:dev`) on a fresh checkout or after any
  `crates/` change before `pnpm dev`/tests"). This was already flagged
  by the 2026-05-26 review and re-flagged in today's sync log; it
  remains unresolved, so flagging again is warranted.

- **`docs/reviews/test-audit-state.json:4`** — `"pr_url": null`. The
  previous state for the 2026-05-27 audit carried
  `"pr_url": "https://github.com/dmagyar-0/driveline/pull/143"`, and
  today's audit was in fact merged via PR #146 (referenced in
  `3e97967`'s commit subject). The state file was written before the
  PR was opened and never backfilled. The field's `null` value is
  ambiguous — does it mean "no PR was created" or "PR exists but
  unknown"? **Fix:** either backfill the actual PR URL after merge, or
  drop the field entirely if the bookkeeping workflow no longer tracks
  it. The current state is internally inconsistent with the commit
  message.

### Nits

- **`docs/sync-logs/2026-05-28-docs-sync.md:13`** — lists "`CLAUDE.md`
  added (project AI-agent context, outside doc allowlist — not edited
  here)" in its commit-considered section. CLAUDE.md was actually added
  in commit `f8c0284` (which is before today's review range but inside
  the sync's wider `85aded8..df8e1ee` window), so the citation is
  range-accurate. Mentioning it merely as "added" while skipping the
  factual-drift finding the sync's own *Reviews consulted* paragraph
  enumerates (line 27) is a bit terse — a one-line "skipped: CLAUDE.md
  drift items remain; needs human pass" entry under *Needs human pass*
  would have closed the loop. Minor process polish, not a bug.

## Per-commit notes

- **`df8e1ee`** chore(review): daily code review 2026-05-27 — Pure
  bookkeeping merge. Adds `docs/reviews/2026-05-27-review.md` (193
  lines) and bumps `daily-review-state.json` from `554f0ba` →
  `7fe2372`. The new report itself is solid: it correctly catches the
  empty-`--only` policy mismatch between the MCAP and MF4 converters,
  the stale header comment in `_demo-comma2k19-video.spec.ts`, the
  stale "four converter invocations" line in
  `build-fixtures.sh`, and the missing SHA256 for `video_seg10.hevc`
  in `EXPECTED_HASHES.txt`. Those remain open Medium/Low items for the
  next code change to address.

- **`3e97967`** test: daily test audit 2026-05-28 (#146) — Pure
  bookkeeping. Adds `docs/reviews/2026-05-28-test-audit.md` (75 lines)
  documenting that the range had no source diff and the baseline holds
  at 447 vitest + 67 cargo green. Bumps `test-audit-state.json` from
  `67ece17` → `df8e1ee` and sets `pr_url: null` (see Low finding
  above).

- **`9428f52`** chore(docs-sync): daily docs sync 2026-05-28 — Three
  effects: (1) rewrites `docs/book/11-run-test-ship.md:72-75` to
  correctly state the wasm output is gitignored — matches
  `.gitignore:19-20`; (2) adds the 60-line
  `docs/sync-logs/2026-05-28-docs-sync.md` documenting which files were
  touched, considered, and flagged for human follow-up; (3) bumps
  `docs/docs-sync-state.json` from `85aded8` → `df8e1ee`. The book
  chapter edit is a strict improvement. The companion bug (same wrong
  claim in `CLAUDE.md`) is acknowledged in the sync log but left in
  place by allowlist policy — see Low finding above.

## Stats

- Files changed: 7
- Lines: +340 / -12
- Production source touched (`apps/web/src/**`, `crates/**/src/**`,
  `*.ts`, `*.tsx`, `*.rs`, `*.py`): 0
- Test files touched: 0
- New markdown reports: 3 (`2026-05-27-review.md`,
  `2026-05-28-test-audit.md`, `2026-05-28-docs-sync.md`)
- JSON state files bumped: 3 (daily-review-state, test-audit-state,
  docs-sync-state)
- Doc chapters edited: 1 (`docs/book/11-run-test-ship.md`)
- Net: no behaviour change to ship.
