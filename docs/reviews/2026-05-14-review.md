# Daily Code Review — 2026-05-14

**Range:** `68b83c6..d9e7b20` (4 commits, 1 merge)

## Summary

Zero source-code changes in the range — another docs/reviews
bookkeeping cycle. The four commits land yesterday's daily review
report (`docs/reviews/2026-05-13-review.md`), today's test audit
report (`docs/reviews/2026-05-14-test-audit.md`), the merge from
PR #113, and today's docs-sync, which contains the only behaviorally
interesting change: four `setCursor` → `advanceCursor` corrections in
`docs/book/10-timeline-and-playback.md` plus a clarifying recap edit
in `docs/book/11-run-test-ship.md`. Verified against
`apps/web/src/timeline/playback.ts:178` — the playback loop does call
`state.advanceCursor(nextNs)`, so the prose now matches the code. No
findings.

## Findings

### Critical

None.

### High

None.

### Medium

None.

### Low

None.

### Nits

None. The docs-sync fix is the right call — the previous prose
conflated the user-scrub seam (`setCursor`, bumps `seekEpoch`) with
the rAF playback seam (`advanceCursor`, does not bump `seekEpoch`).
Spot-checked the corrected lines against the source: `playback.ts:174-178`
explicitly documents the two-seam contract and matches the new prose
verbatim ("same clamp + auto-pause semantics as `setCursor`, but it
does not bump `seekEpoch`"). Clean.

## Per-commit notes

- **`85aded8`** chore(review): daily code review 2026-05-13
  (`9d2055e..68b83c6`). Adds `docs/reviews/2026-05-13-review.md`
  (74 lines) and bumps `daily-review-state.json` to
  `68b83c6` / `2026-05-13T03:11:30Z`. Report is internally consistent:
  the prior range was docs/state only, and the report says so. Per-
  commit notes correctly identify the four commits in `9d2055e..68b83c6`
  (`9e2fd84`, `1b483bd`, `ca257e9` merge, `68b83c6`). Docs-only; clean.
- **`c0c0114`** test: daily test audit 2026-05-14 (`9e2fd84..85aded8`).
  Adds `docs/reviews/2026-05-14-test-audit.md` (63 lines) and updates
  `test-audit-state.json` to `85aded8` / `2026-05-14T02:05:00Z` with
  `pr_url: null` (PR opened separately, recorded by the merge in the
  next commit). Baseline numbers reported (web 447 / rust 67) match the
  state of `main` at that SHA — both suites are unchanged from the
  prior audit because no source files changed. Docs-only; clean.
- **`83b5f79`** Merge commit for PR #113 (test-audit-report/2026-05-14).
  No new content; standard merge.
- **`d9e7b20`** chore(docs-sync): daily docs sync 2026-05-14
  (`1afd5da..85aded8`). Touches two book chapters plus
  `docs/docs-sync-state.json` and adds `docs/sync-logs/2026-05-14-docs-sync.md`.
  Substantive change is the `setCursor` → `advanceCursor` correction
  in four spots in `10-timeline-and-playback.md` (lines 194, 207, 224,
  237) and the recap bullet in `11-run-test-ship.md` (lines 257-259).
  Both edits are factually correct — `playback.ts:178` calls
  `state.advanceCursor(nextNs)`, not `setCursor`, and the distinction
  matters because `advanceCursor` is the seam that skips `seekEpoch`
  to avoid video-decoder seek storms during playback. Sync log
  (`docs/sync-logs/2026-05-14-docs-sync.md`) accurately enumerates the
  no-op-considered files. Docs-only; clean.

## Stats

- Files changed: 8
- Inserted: +250
- Deleted: −16
- Source files (`apps/`, `crates/`, `scripts/`): 0
- Test files touched: 0
- New dependencies: none
- Docs drift: net negative — the docs-sync commit eliminates a known
  inaccuracy (`setCursor`/`advanceCursor` confusion in the book) and
  introduces no new drift.
